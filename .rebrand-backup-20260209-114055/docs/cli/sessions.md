---
summary: "CLI reference for `autolab sessions` (list stored sessions + usage)"
read_when:
  - You want to list stored sessions and see recent activity
title: "sessions"
---

# `autolab sessions`

List stored conversation sessions.

```bash
autolab sessions
autolab sessions --active 120
autolab sessions --json
```
